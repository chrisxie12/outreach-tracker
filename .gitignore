# Secrets and local environment — never commit
.env
.env.local
.env.*
!.env.example
.dev.vars
*.dev.vars
wrangler.local.toml
.wrangler/

# OS / editor noise
.DS_Store
Thumbs.db
.idea/workspace.xml

# Dependencies
node_modules/

# Tool caches
node_modules/.cache/